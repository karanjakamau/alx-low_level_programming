#include "holberton.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: pointer to char type
 * Return: integer
 */

int _strlen(char *s)
{
int i;
i = strlen(s);
return (i);
}
