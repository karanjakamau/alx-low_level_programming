#include <stdlib.h>
#include <stdio.h>

/**
 * main - check the statment if
 * is positive or negative.
 * Return: the function returns 0
 */
int main(void)
{
    int i;
    for (i = '0'; i < 'F'; i++)
    {
        putchar(i);
    }
    putchar('\n');
    return(0);
}
