#include <stdio.h>
#include <stdlib.h>

/**
 * main - print digits
 * of base 10.
 * Return: the function returns 0
 */
int main(void)
{
int i;

for (i = 0; i < 10; i++)
{
printf("%d", i);
}
putchar('\n');
return (0);
}
